var responseParser = {
    parse: function (html) {
        var results = { results:[{"text": "AngularJS — Superheroic JavaScript MVW Framework"}]};
        return results;    
    }
};

module.exports = responseParser;
